PS Move API for Pocket C.H.I.P
==============================

To install the build-dependencies needed to build from source:

    bash scripts/pocketchip/install_dependencies.sh


To build from source:

    bash scripts/pocketchip/build.sh


To be able to interface with the Move Controller as normal user:

    sudo cp contrib/99-psmove.rules /etc/udev/rules.d/
